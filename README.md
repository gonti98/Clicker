# Clicker

A simple terminal app in Python based on `curses`.

## Installation and usage

```bash
python -m venv .venv
source .venv/bin/activate
pip install --editable .
pip install --upgrade pip
clicker
```

```bash
source .venv/bin/activate
pip install --editable ".[test]"
pip install --upgrade pip
pytest -vv
```
